# Portfolio agent instructions

Read docs/HANDOFF.md before editing this repository.

After EVERY completed portfolio edit (code, content, styling, assets, configuration or bug fix), update docs/HANDOFF.md and regenerate docs/portfolio-handoff.pdf with `python3 scripts/build_handoff.py` (requires reportlab). This is a standing instruction from Shanthanu Kelso dated 2026-09-21; do not wait for another request.

Update current state and append a dated change-log entry with affected files, reason, actual checks, deployment status and unresolved issues. Preserve this instruction in every handoff edition. Render and visually inspect every PDF page before delivery. Commit the handoff with related changes when practical; report explicitly if blocked. Verify deployment separately; never claim unperformed checks passed.

Use targeted edits, preserve user copy/assets, keep titles upright/off-white and use SVG/CSS icons rather than emojis. Verify current branch and source before editing. Do not force-push or overwrite unrelated work.
